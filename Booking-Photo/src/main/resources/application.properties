spring.application.name=Booking-Photo

# --- Cấu hình Database (Lấy từ Environment Variables của Render) ---
spring.datasource.url=jdbc:mysql://${DATABASE_HOST:mysql-bkpt-dbbk123.g.aivencloud.com}:${DATABASE_PORT:28849}/${DATABASE_NAME:defaultdb}?useSSL=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&testOnBorrow=true
spring.datasource.username=${DATABASE_USER:avnadmin}
# Render sẽ lấy giá trị từ biến DATABASE_PASSWORD bạn đã nhập
spring.datasource.password=${DATABASE_PASSWORD}

# --- Cấu hình HikariCP giúp tự hồi sinh kết nối ---
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.maximum-pool-size=5

# --- JPA / Hibernate ---
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- Cấu hình gửi Mail OTP (Lấy từ Environment Variables của Render) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:baotoanit1505@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8

# --- Bảo mật JWT ---
jwt.signerKey=${JWT_SIGNER_KEY:xJTUVPssT2uOmsKKn7dmwi/ZuUg1b8ECkPuoSBYziE4ldzMBtSNkaftz1U372J/e}
